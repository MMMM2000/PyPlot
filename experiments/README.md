# Experiments

This directory collects experimental prototypes that are not part of the core
application.  The `data_plotter.py` script provides a simple GUI to select one of
the existing plotting tools, choose data files and optionally check for outliers
before launching the plotter.

Run it with:

```bash
python experiments/data_plotter.py
```
