- Show the TMA `Update remaining sweeps` action for pending runtime edits during
  dedicated-process recipes, including disabling current hold while the active
  sweep is recovering the stress target.
