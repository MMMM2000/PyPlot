TMA and Current Annealing now capture Git/source provenance exactly once per run or prepared output on a daemon task, preventing source-control queries from delaying control or GUI threads. Metadata retains the existing source-control keys and adds explicit pending/completed/unavailable timestamps and states, including `dirty_state` values that distinguish unknown capture results from a clean worktree.
