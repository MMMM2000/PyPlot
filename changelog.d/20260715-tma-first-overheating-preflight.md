- Warn before TMA hardware preflight when first overheating is disabled and no exact previous TMA measurement is cached for the wire, with configure, explicit-continue, and cancel actions plus durable skip metadata/logging; use conservative specimen matching for real Builder TMA rows, accept only finalized run metadata as history, and run cancellable history scans as daemon Python tasks that cannot block shutdown or retain the closed window.
