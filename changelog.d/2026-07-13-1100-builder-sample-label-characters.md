2026-07-13 11:00

- Kept microscope filename notes such as `No1` and `noload` grouped with the regular microwire while preserving `oe` as a separately hideable other-end identity.
- Restored the proper `Ω`, `µm`, and superscript `²` characters in current-annealing graph axis labels.
