2026-03-14 01:20
- Expanded the figure-layout workflow with panel-label placement/size controls, style presets, minor-tick/tick-direction/scientific-notation controls, manual tick lists, and reusable figure-template save/load support.
- Added manuscript-oriented workflow actions for `Clone figure`, `Figure sources`, and deterministic `Export all figures...` batch export, alongside additional paper export formats (`EPS`) and transparent PNG export.
- Added callout-box support plus copy/paste/duplicate annotation tools so paper annotations can be reused more efficiently across graph revisions and panels.
