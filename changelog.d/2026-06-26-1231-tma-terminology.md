2026-06-26 12:31
- Rename user-facing TMA wording to TMA across the Builder, PyPlot plug-in, logger help, report labels, and export headers while keeping legacy `mini_dma` payload keys, modules, folders, and CLI workflows compatible.
- Add the `--tma-bench-plan` launcher alias while preserving the existing `--mini-dma-bench-plan` flag.
