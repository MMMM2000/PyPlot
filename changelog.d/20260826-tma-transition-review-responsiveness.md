Fixed TMA transition review loading and large Builder project saving so source parsing and payload staging no longer starve the GUI thread. Transition choices now preselect automatic values or **Not observed**, no-cooling TMA sweeps ask only for As/Af, heating-only current-annealing reviews remain completed after saving, and selected markers can be dragged directly into manual mode. Closing a TMA review now mirrors only newly saved payloads without reopening source folders, repaints before refreshing the overview, and filters unsupported run modes in the background instead of opening them as load failures.
