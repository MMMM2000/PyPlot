### Fixed

- Load the correct packaged project data when switching or refreshing the Annealing, VSM, and TMA transition pages, and show visible progress plus a retryable error message while deferred data loads.