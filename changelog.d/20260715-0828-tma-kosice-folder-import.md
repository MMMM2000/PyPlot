2026-07-15 08:28 UTC
- Added an independently saved, cancellable background Košice-folder index to the TMA Sample tab, including conservative filename matching and visible match/source status.
- Made Show annealing choose matching sources in `.pydpj`, Košice, then fabrication-folder order while preserving the existing stacked preview and identifying its source.
- Added read-only parsers for Košice three-column `.txt`, legacy four-column `.dat`, and current six-column `.dat` curves; discovered Origin `.opju` content remains explicitly unsupported.
