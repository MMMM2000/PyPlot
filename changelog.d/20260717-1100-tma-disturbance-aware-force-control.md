- Made Prague and Košice iso-stress recovery treat transformation and thermal contraction as an active disturbance instead of evidence that a correctly directed motor correction failed.
- Removed the Košice sub-resolution-response recipe stop, the hidden 3 mm current-sweep travel setting, and the obsolete bench millimetre override. Recovery now remains bounded by per-command strain, strain-rate, target-relative, motor, and physical stage safeguards.
- Added trend-projected Košice recovery, confirmed motor direction reversals, readability-aware outlier rejection with a robust detrended Prague endpoint estimate, strict noise-independent heating-resume tolerances, far-error Prague ramp cruise, coherent transformation-drift chaining during Prague current-hold recovery, and a fluctuating transformation simulator family.
- Added shared Prague/Košice Tic command receipts and target-position readback, prioritized target moves over coalesced keepalives, and made Košice require exact stationary completion only after the Tic confirms the requested target. Failed, delayed, or ignored commands are now released for retry from the confirmed position instead of creating a permanent recovery hold.
- Made exact Tic target readback authoritative across device-specific planning-mode values and kept status polling active for pending/manual motor commands, preventing accepted Košice manual moves from being falsely rejected while the app is outside a recipe.
- Changed held manual jogging from repeated position/readback handshakes to one native Tic velocity command with priority halt-and-hold on release, while retaining exact position steps for quick clicks.
- Made Tic target-acceptance timeout use monotonic time and release an unconfirmed command even if no post-dispatch status timestamp is available, so a submitted-but-ignored target is retried instead of holding setup or recovery indefinitely.
- Added Tic operation state, active errors, transport, and VIN to every control-trace row so future controller-side command rejection is distinguishable from transport and mechanical failures.
