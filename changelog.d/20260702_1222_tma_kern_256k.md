2026-07-02 12:22
- Added 256000 and 128000 baud KERN KCP scale support so the TMA logger can auto-detect and preserve high-speed EWJ/TEWJ USB settings.
- Updated scale presets so Prague G&G uses the legacy 9600 baud ESC+p / 250 ms settings while Košice KERN uses 256000 baud SI / 50 ms settings.
- Fixed stale auto-generated sample names with split wire tokens so current naming fields update the Sample name and recipe header.
- Fixed hardware auto-connect freezing on scale detection by using a short selected/scale-like port probe path instead of the full serial scan.
- Applied direct HMP4030/HMP4040 TMA defaults during auto-connect so CH2 motor power is enabled before checking Tic VIN.
