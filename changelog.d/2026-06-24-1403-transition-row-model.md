2026-06-24 14:03
- Made the Builder `Transitions` workspace use graph-level rows for Annealing and VSM, with repeated sample grouping and concise graph labels.
- Replaced the DMA transition summary panel with run/target rows that expose review status and counts using the same visible concepts as Annealing and VSM.
